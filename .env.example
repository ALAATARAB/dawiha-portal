# Dawiha API (host only — the app calls `{this}/api/v1/...`).
# Override for local or other environments; if unset, dev default matches env-config.
VITE_API_BASE_URL=https://dawiha-server.vercel.app

# Shown in the shell branding.
VITE_APP_NAME=Dawiha Admin
