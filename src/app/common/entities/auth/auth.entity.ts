import type { FullAuthenticationEntity } from './me.entity'

/**
 * AuthEntity - Login response (alias for FullAuthenticationEntity)
 * Based on dawiha-server swagger.yaml
 */
export type AuthEntity = FullAuthenticationEntity
